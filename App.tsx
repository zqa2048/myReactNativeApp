// export { default} from "./src/pages/home"import React from "react"import {  View,  Text,  StyleSheet,  Image,  Dimensions,  StatusBar,  TextInput,  FlatList, TouchableOpacity} from "react-native";export default function index(){  const window = Dimensions.get("window")  const fn=()=>{    console.log(window)    }  // fn()  return (    <View style={styles.container}>      <StatusBar        barStyle='light-content'      >      </StatusBar>      <Image        source={require('./resource/imgs/logo.jpg')}        style={{width:window.width,height:window.height / 4}}      />      <TextInput        placeholder='请输入搜索内容'        style={[styles.search,{top:window.height / 4 * .7}]}        placeholderTextColor='#000'      >      </TextInput>      <FlatList        data={[{ name: "zhangsan", key: '1' },{name:'lisi',key:'2'}]}        style={styles.content}        renderItem={({ item, index })=>{          return (            <TouchableOpacity              onPress={()=>console.log(index)}            >              <Text>                {item.name}              </Text>            </TouchableOpacity>          )        }}      >      </FlatList>    </View>  )}const styles = StyleSheet.create({  container:{    flex:1,    backgroundColor:"#eee",    // paddingTop:20,  },  search:{    position:'absolute',    left:20,    right:20,    backgroundColor:"#A9F5F2",    color:'#000',    borderRadius:5,    height:40,    lineHeight:40,    paddingLeft:10  },  content:{    flex:1,    backgroundColor:"red",    marginTop:10,    marginLeft:10,    marginRight:10,    borderRadius:10,    paddingLeft:10  }})