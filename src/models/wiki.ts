import { Reducer } from "react";import { homeList } from "../service/ProjectService";import { Effect } from "./modelsType";export interface IWikiModelsState {	categories:[]}export interface IWikiModelsType {	namespace:'wiki';	state:IWikiModelsState;	effects:{		getMyList:Effect	};	reducers:{		save:Reducer<IWikiModelsState,any>	}}const wiki:IWikiModelsType = {	namespace:'wiki',	state:{		categories:[]	},	reducers:{		save(state, { payload }) {			// console.log(state,payload)				return {...state,...payload}			},	},	effects:{		/**		 * 说明： 获取我的首页菜单		 * @author zhou		 */		*getMyList(payload,{ call, put }){			const { data } = yield call(homeList)			yield put({				type:'save',				payload:{					categories:data.categories				}			})		}	}}export default wiki