import { Button, Modal } from "@ant-design/react-native";import React, { useState } from "react";import { Text, View } from "react-native";import { useDispatch, useSelector } from "react-redux";const Index =()=>{	const [visible, setVisible] = useState(false)		const footerButtons = [		{ text: 'Cancel', onPress: () => console.log('cancel') },		{ text: 'Ok', onPress: () => console.log('ok') },	];	const dispatch = useDispatch()	// @ts-ignore	 const count = useSelector(({ count: count })=>count)	return (		<View>				<Button					onPress={()=> {						Modal.alert("modal",'吃了吗',[							{								text:'cancel',								onPress:()=>{}							},							{								text:'OK',								onPress:()=>{}							}						])											}}				>					Modal				</Button>				<Modal					title="Title"					transparent					onClose={()=>setVisible(false)}					maskClosable					visible={visible}					closable					footer={footerButtons}				>					<View style={{ paddingVertical: 20 }}>						<Text style={{ textAlign: 'center' }}>Content...</Text>						<Text style={{ textAlign: 'center' }}>Content...</Text>					</View>					<Button type="primary" onPress={()=>setVisible(false)}>						close modal					</Button>				</Modal>								<Text>{count}</Text>		<Button			onPress={()=>dispatch({type:'count/add'})}		>			+		</Button>		<Button			onPress={()=>dispatch({type:'count/minus'})}		>			-		</Button>		<Button			onPress={()=>dispatch({type:'count/addWithDelay'})}		>			+ async		</Button>		</View>	)} export default Index