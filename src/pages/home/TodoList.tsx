import React,{useState} from 'react'import {  Text,  StyleSheet,  TouchableOpacity,  ScrollView,  FlatList} from "react-native";interface IProps {  data:{    list:[]  },  onClick:(index:number)=>void}const Index:React.FC<IProps> = ({data,onClick}) => {const fn=()=>{  console.log("data3",data?.list)}fn()  return (      <FlatList        style={styles.container}        data={data?.list}        initialNumToRender={7}        renderItem={({ item, index })=>{          return (            <TouchableOpacity              onPress={()=>onClick(index)}            >              <Text                style={styles.item}              >                {item}              </Text>            </TouchableOpacity>          )        }}        keyExtractor={({ index })=>index}      >      </FlatList>  )}const styles = StyleSheet.create({  container: {    marginLeft:30,    height:210    // backgroundColor:"#ccc"  },  item:{    marginTop:10,  }})export default Index