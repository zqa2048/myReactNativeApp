import React, { useEffect, useState } from "react";import { AsyncStorage, StatusBar, Switch, Text, View } from "react-native";import { useDispatch, useSelector } from "react-redux";import styles from './styles'const User = () => {		// @ts-ignore	const home = useSelector(({home}) => home)	const dispatch = useDispatch()			const onTabChange = (value:boolean) => {		dispatch({			type: 'home/changeNear',			payload:{				type: 'change',				value:value			}		})	}						return (		<View			style={styles.wrapper}		>			<StatusBar				barStyle='light-content'			>			</StatusBar>			<Text style={styles.title}>是否显示附近美食</Text>			<Switch value={home.nearSwitch} onValueChange={(value => {onTabChange(value)})}/>		</View>	)}export default User