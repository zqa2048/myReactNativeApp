// export { default} from "./src/pages/home"import React,{ useEffect, useState} from "react"import {	View,	Text,	StyleSheet,	Image,	Dimensions,	StatusBar,	TextInput,	FlatList,	TouchableOpacity,} from "react-native";import { TabBar, Provider, Button, Modal } from "@ant-design/react-native";import { AntDesign } from '@expo/vector-icons';import {  useNavigation } from "@react-navigation/native";import { URL } from "../../constants/url";export default function index() {		const window = Dimensions.get("window")	const WidthItem = (window.width - 20) / 3 -20	const HeightItem = (window.height * .75 - 90) / 4	const IMG = (window.width - 20) / 3 - 20	const fn=()=>{		console.log(window)	}	// fn()	const navigation = useNavigation()	const GoTo = (params:{}) => {		navigation.navigate('List',{data:params})	}	const [categories,setCategories] = useState([])	const bannerImg = require('../../../resource/imgs/banner.png')	useEffect(()=>{		fetch(URL.index)			.then((res)=>res.json())			.then(res=> {				if(res.ret && res.data){					setCategories(res.data.categories)				}			})			.catch((err)=>alert("请求异常,请重试!:"))	},[])		return (		// <Provider>		<View style={styles.container}>			<StatusBar				barStyle='light-content'			>			</StatusBar>			<Image				source={bannerImg}				style={{width:window.width,height:window.height / 4}}			/>			<TextInput				placeholder='请输入搜索内容'				style={[styles.search, { top: window.height / 4 * .7 }]}				placeholderTextColor='#000'			>			</TextInput>						<View				style={styles.content}			>				<FlatList					data={categories}					initialNumToRender={12}					numColumns={3}					showsHorizontalScrollIndicator={false}					showsVerticalScrollIndicator={false}					keyExtractor={(item)=>item["id"]}					renderItem={({ item, index })=>{						return (							<TouchableOpacity								onPress={()=>GoTo(item)}								style={									[										{											width:WidthItem,											height:HeightItem ,											marginTop:8,											marginLeft:10,											marginRight:10,										}									]								}							>								<Image									source={{uri:item["imgUrl"]}}									style={{										width:IMG,										height:IMG,										borderRadius:5									}}								/>								<Text style={styles.itemTitle}>									{item["title"]}								</Text>							</TouchableOpacity>						)					}}				>				</FlatList>				{/*<Button type='primary'>下一个</Button>*/}			</View>		</View>		// </Provider>	)}const styles = StyleSheet.create({	container:{		flex:1,		backgroundColor:"#eee",	},	search:{		position:'absolute',		left:20,		right:20,		backgroundColor:"#A9F5F2",		color:'#000',		borderRadius:5,		height:40,		lineHeight:40,		paddingLeft:10	},	content:{		flex:1,		backgroundColor:"#FFF",		marginTop:10,		marginLeft:10,		marginRight:10,		borderRadius:10,		marginBottom:10,	},	itemTitle: {		textAlign: 'center'	}	})